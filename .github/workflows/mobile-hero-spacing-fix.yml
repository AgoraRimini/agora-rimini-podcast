name: Fix mobile hero spacing (manuale)

# Workflow storico mantenuto solo per riferimento.
# Non deve più avviarsi automaticamente: il layout mobile è già nel CSS
# e la pubblicazione è gestita esclusivamente da pages.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow archiviato
        run: echo "Il fix mobile è già applicato; nessuna modifica necessaria."
